import * as constants from '../constants'

// Nothing to see here, move along
const init = {}

export const user = (state = init, action) => {
  switch(action.type) {
    default:
      return state
  }
}